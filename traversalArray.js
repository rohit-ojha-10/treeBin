const array = document.getElementById("textToCopy");
array.setAttribute("class", "traversal-array");

let arr = []; // initializing state to be accessed by all traversals and could be used across application, also to reset!
